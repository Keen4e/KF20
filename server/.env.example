# Provider selection stays server-side. Never commit real values.
AI_PROVIDER=openai
AI_MODEL=gpt-5.6
OPENAI_API_KEY=replace-with-secret-store-reference

# Prototype-only shared token. Replace with user authentication before external tests.
KF20_API_TOKEN=replace-with-long-random-token
KF20_ALLOWED_ORIGIN=
KF20_RATE_LIMIT=30
PORT=8787

